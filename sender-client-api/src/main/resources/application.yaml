server:
  port: 8080

spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost/sender-app
    username: postgres
    password: alex
  jpa:
    hibernate:
      ddl-auto: update